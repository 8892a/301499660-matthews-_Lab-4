trigger: none

resources:
  pipelines:
  - pipeline: ciBuild
    source: 8892a.301499660-matthews-_Lab-4
    trigger: true

stages:
- stage: Deploy
  displayName: Deploy to Lab4 Environment
  jobs:
  - deployment: DeployWebApp
    displayName: Deploy ASP.NET Core Web App
    environment: 'Lab4-Environment'
    pool:
      name: Default

    strategy:
      runOnce:
        deploy:
          steps:

          - download: ciBuild
            artifact: drop

          - task: UseDotNet@2
            inputs:
              packageType: 'sdk'
              version: '8.x'