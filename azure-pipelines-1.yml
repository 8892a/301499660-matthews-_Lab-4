trigger: none

resources:
  pipelines:
  - pipeline: ciBuild
    source: 8892a.301499660-matthews-_Lab-4
    trigger: true

pool:
  name: Default

steps:
- download: ciBuild
  artifact: drop

- task: UseDotNet@2
  inputs:
    packageType: sdk
    version: 8.x

- script: |
    echo "Artifact contents:"
    dir $(Pipeline.Workspace)\ciBuild\drop
  displayName: Show artifact

- script: echo "Deployment successful"
  displayName: Deploy Step